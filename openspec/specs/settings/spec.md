# Settings Spec

## Overview
A settings page to centralise all user-configurable options.

## Current Settings

| Setting | Default | Source |
|---|---|---|
| Load subfolders when opening a folder | Off | file-import spec |
| Thumbnail cache size limit | 200 MB | thumbnail-cache spec |
| Clear thumbnail cache (action) | — | thumbnail-cache spec |

## Notes
- This list will grow as new features are added
- Settings are persisted to disk (config file in OS-standard config directory)
- Settings survive app restarts
