# Init Tauri Project — Tasks

- [x] Initialize git repository
- [x] Scaffold Tauri project with Svelte template using `create-tauri-app`
- [x] Add `image` crate to Rust dependencies
- [x] Add `leaflet` to frontend dependencies
- [x] Update `tauri.conf.json` with app metadata (name: Spool, identifier, window title)
- [x] Verify the app builds and runs on macOS
